


# config.yaml — Three-node cluster: 1 control plane + 2 workers
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
     - containerPort: 30012
       hostPort: 8080
  - role: worker
  - role: worker